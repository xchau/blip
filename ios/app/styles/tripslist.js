import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  caption: {
    fontSize: 20,
    fontWeight: 'bold',
    alignItems: 'center',
  },
  searchContainer: {
    alignItems: 'center',
    height: 50,
    marginBottom: 10
  },
  listContainer: {
    alignItems: 'center',
    backgroundColor: '#fff',
    flex: 1,
    flexDirection: 'column',
  },
});
